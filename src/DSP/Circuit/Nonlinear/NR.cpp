#include "NR.h"

// All NRPolicy logic is implemented in the header via template methods.
// This translation unit exists to satisfy the .h/.cpp source convention and
// to allow non-template, non-inline members to be added here in the future.
