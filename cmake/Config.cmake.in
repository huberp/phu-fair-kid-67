@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Provide the same variables as the old FindPhuFairchildModels.cmake
set(PhuFairchildModels_VERSION @PROJECT_VERSION@)

# Our library dependencies (contains the imported targets)
include("${CMAKE_CURRENT_LIST_DIR}/PhuFairchildModelsTargets.cmake")

# These are IMPORTED targets created by PhuFairchildModelsTargets.cmake
check_required_variables(Phu::PhuFairchildModels)

# Report the variables we've set
set(PhuFairchildModels_FOUND TRUE)
set(PhuFairchildModels_VERSION @PROJECT_VERSION@)
